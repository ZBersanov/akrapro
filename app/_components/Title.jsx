const Title = ({ title }) => {
  return (
    <div
      className="font-grandis text-[36px] sm:text-[44px] md:text-[52px] lg:text-[65px]
        text-white uppercase text-center font-semibold py-4"
    >
      {title}
    </div>
  );
};

export default Title;
